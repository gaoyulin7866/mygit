<view class="page">
  <view class='cont' wx:for="{{arr}}" wx:key="{{index}}">
    <text class='time'>Time: {{item.time}}</text>
    <text class='step'>Step: {{item.step}}</text>
  </view>
  <view class="body-view">
    <loading hidden="{{hidden}}" bindchange="loadingChange">
      加载中...
    </loading>
  </view>
</view>
